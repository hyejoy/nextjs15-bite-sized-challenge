# Section 03-02. 페이지 라우팅 설정하기

## 📝 쿼리 스트링(Query String) 설정

- 쿼리 스트링은 **페이지의 경로(Path)에 영향을 주지 않음**
- 주로 검색어, 필터, 정렬 조건 등에 사용됨

---

### 1. Pages Router 환경

Next.js **Pages Router**에서는 `next/router`의 `useRouter`를 사용합니다.

```ts
/**
 * Pages Router – Query String 사용 방법
 *
 * 예시 URL
 * http://localhost:3000/search?q=최수수
 *
 * router.query 객체에서 쿼리 스트링을 추출할 수 있음
 */

⭐ import { useRouter } from 'next/router';

export default function Page() {
  ⭐ const router = useRouter();
  console.log(router);

  ⭐ const { q } = router.query; // '최수수'
  console.log(q);

  return <div>검색어: {q}</div>;
}
```

2. 참고) App Router 환경
   Next.js App Router에서는 next/navigation의 useRouter를 사용
   import { useRouter } from "next/navigation";

## 📝 URL 파라미터 (동적경로) 파일 생성하기

```bash
section03/
├─ 📂pages/
│  ├─ 📂book/
│  └─  └─ [id].tsx
```

```ts
import { useRouter } from "next/router";

export default function Page() {
  const router = useRouter();
  console.log(router);
  const { id } = router.query;

  return (
    <>
      <h1>BOOK{id}</h1>
    </>
  );
}
```

## 📝 URL 복수 파라미터 (동적경로) 파일 생성하기

- [...param].tsx 형태로 생성≒캐치 올 세그먼트(Catch All Segment)
- localhost:3000/book/244/244/344....

```bash
section03/
├─ 📂pages/
│  ├─ 📂book/
│  └─  └─ [].tsx
```

```ts
const { id } = router.query;
```

- 캐치 올 세그먼트를 사용하면 id는 배열형태로 넘어옴

## 캐치 올 세그먼트가 대응할 수 없는 예외 페이지

- localhost:3000/book 과같이 인덱스 경로로 요청이 오면 대응X

## 해결법

1. `book/index.tsx` 생성해주기
2. `[[...id]].tsx` 로 변경하기 ≒ 옵셔널 캐치 세그먼트
   └── /book에도 대응하는 옵셔널 캐치 세그먼트 표현

## 404 페이지 만들기

- `src/pages/404.tsx` 생성하기

```

```
